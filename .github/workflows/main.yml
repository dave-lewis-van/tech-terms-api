name: API Documentation Pipeline

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    
jobs:
  validate-and-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # 1. GENERATION: Use Python to export the spec from FastAPI
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Python dependencies
        run: |
          pip install -r requirements.txt

      - name: Generate OpenAPI Spec
        run: |
          python gen_openapi.py  # This outputs your openapi.json

      # 2. LINTING: The "Schematron" for OpenAPI
      - name: Lint OpenAPI Spec
        uses: stoplightio/spectral-action@v0.8.13
        with:
          file_glob: 'openapi.json'

      # 3. CONTRACT TESTING: Ensuring the Spec and Code haven't drifted
      # This runs the API locally and tests it against the spec
      - name: Run Schemathesis Tests
        run: |
          uvicorn main:app --host 0.0.0.0 --port 3000 &
          sleep 5
          schemathesis run ./openapi.json --url http://localhost:3000 | tee schemathesis-report.txt

      - name: Upload Schemathesis Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: schemathesis-report
          path: schemathesis-report.txt

      # 4. SYNC: Push the validated spec to ReadMe
      - name: Sync to ReadMe
        uses: readmeio/rdme@v8
        with:
          rdme: openapi ./openapi.json --key=${{ secrets.README_API_KEY }}